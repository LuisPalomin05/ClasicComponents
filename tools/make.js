const extnfile = [
    ".html", ".htm", ".xhtml",
    ".css", ".scss", ".sass", ".less", ".styl",
    ".js", ".jsx", ".ts", ".tsx", ".mjs",
    ".svg",
    ".json",
    ".xml",
  ];
  
  for (let typefile = 0; typefile < extnfile.length; typefile++) {
      const element = extnfile[typefile];
      console.log(element);  // Aquí puedes realizar la acción que desees con cada extensión
  }
  